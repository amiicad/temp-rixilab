<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Super Admin Dashboard</title>
  <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css" />
  <link rel="stylesheet" href="/css/style.css" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" />

  <link href="img/Rixi Lab New Logo PNG.png" rel="icon" />
  <link href="img/apple-touch-icon.png" rel="apple-touch-icon" />

  <style>
     body { overflow-x: hidden; background-color:#ecfeff; }
     .sidebar { width: 250px; }
    .sidebar { min-height: 100vh; background: #343a40; color: white; padding-top: 20px; }
    .sidebar a { color: white; text-decoration: none; display: flex; align-items: center; padding: 12px 20px; border-radius: 6px; margin-bottom: 5px; }
    .sidebar a:hover { background: #495057; }
    .sidebar i { margin-right: 10px; }
    .main-content { margin-left: 250px; padding: 30px; }
    /* .card:hover { transform: scale(1.03); transition: 0.3s; } */
    .badge-pending { background-color: #ffc107; color: black; }
    .badge-approved { background-color: #28a745; }
    .badge-rejected { background-color: #dc3545; }
    .table td, .table th { vertical-align: middle; }
    .section-title { font-weight: 600; margin-bottom: 15px; }
    .modal-header { background-color: #343a40; color: white; }
    .sidebar a.active {
     background-color: #ff6600;  /* Bootstrap primary */
     color: #fff;
     font-weight: bold;
}
  </style>
</head>
<body>
  <div class="d-flex">
  <button class="btn btn-primary d-md-none" id="sidebarToggle" style="position: fixed; top: 10px; left: 10px; z-index: 1050;">
  <i class="bi bi-list"></i>
</button>
    <!-- Sidebar -->
  <div class="sidebar position-fixed w-20 d-flex flex-column justify-content-between" style="height: 100vh;">
  <div>
    <h2 class="text-center">
      <img src="/img/Rixi Lab New Logo PNG.png" width="35px" style="margin-bottom: 5px;" alt="Logo"> Rixi Lab
    </h2>
    <h4 class="text-center">Super Admin Panel</h4>
    <br/>
    <a href="#dashboard"><i class="bi bi-person-lines-fill"></i> Dashboard</a>
    <a href="#viewAdmin"><i class="bi bi-person-workspace"></i> View Admins</a>
    <a href="#viewInterns"><i class="bi bi-people"></i> View Interns</a>
    <a href="#createAdmin"><i class="bi bi-person-plus"></i> Create Admin</a>
    <a href="#createAdmin"><i class="bi bi-envelope-plus"></i> Confirmation Mail</a>
    <a href="#createAdmin"><i class="bi bi-envelope-plus"></i> Completion Mail</a>
    <a href="/logout"><i class="bi bi-box-arrow-right"></i> Logout</a>
  </div>

  <!-- Footer at the bottom -->
  <div class="text-center mb-2" style="font-size: 0.85rem; color: #888;">
    © Copyright 2025 Rixi Lab.<br/> All Rights Reserved <br/><span style="font-size: smaller;">Developed by Anunay Anand (Director)</span>
  </div>
</div>

    <!-- Main Content -->
    <div class="main-content flex-grow-1">

      <!-- Welcome Message -->
      <div class="mb-4">
        <h4 class="section-title mb-4">Welcome to Rixi Lab, <%= superAdmin.name %>, <%=superAdmin.designation%></h4>
      </div>


      <!-- Dashboard -->
            <section id="dashboard" class="py-4">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h4 class="fw-bold mb-0">Super Admin Dashboard</h4>
    <!-- Admin Profile -->
    <div class="d-flex align-items-center gap-3">
      <img src="<%= superAdmin.img_url %>" 
           alt="Admin Image" 
           class="rounded-circle shadow-sm" 
           style="width:60px; height:60px; object-fit:cover;">
    </div>
  </div>

  <!-- Stats Row -->
  <div class="row g-4">
    <div class="col-md-3">
      <div class="card  shadow-sm h-100 text-center p-3"style="border-color: #ff6600 !important;" >
        <div class="card-body" >
          <h6 class="text-muted">Total Admins</h6>
          <p class="display-5 fw-bold text-primary mb-0"><%= admins.length %></p>
        </div>
      </div>
    </div>
    <div class="col-md-3">
      <div class="card  shadow-sm h-100 text-center p-3" style="border-color: #ff6600 !important;">
        <div class="card-body">
          <h6 class="text-muted">Total Interns</h6>
          <p class="display-5 fw-bold text-success mb-0"><%= interns.length %></p>
        </div>
      </div>
    </div>
    <div class="col-md-3">
      <div class="card  shadow-sm h-100 text-center p-3" style="border-color: #ff6600 !important;">
        <div class="card-body">
          <h6 class="text-muted">Total Batches</h6>
          <p class="display-5 fw-bold text-info mb-0"><%= batches.length %></p>
        </div>
      </div>
    </div>
     <div class="col-md-3">
      <div class="card  shadow-sm h-100 text-center p-3" style="border-color: #ff6600 !important;">
        <div class="card-body">
          <h6 class="text-muted">Certified Interns</h6>
          <p class="display-5 fw-bold text-info mb-0"><%= certifiedInternsCount  %></p>
        </div>
      </div>
    </div>
  </div>
</section>

      </section>
      <!-- Create Admin -->
<section id="createAdmin" style="display:none;">
  <h4 class="section-title">Create Admin</h4>
  <div class="card p-4 shadow-sm mb-4 " style="border-color: #ff6600 !important;">
    <form action="/create-admin" method="POST" class="row g-3 needs-validation" novalidate>
      
      <!-- First Row -->
      <div class="col-md-4">
        <input type="text" name="name" class="form-control" placeholder="Name" required>
      </div>
      <div class="col-md-4">
        <input type="email" name="email" class="form-control" placeholder="Email" required>
      </div>
      <div class="col-md-4">
        <input type="text" name="password" class="form-control" placeholder="Password" required>
      </div>

      <!-- Second Row -->
      <div class="col-md-4">
        <select name="domain" class="form-select" required>
          <option value="" disabled selected>Domain</option>
          <option value="Web Development">Web Development</option>
          <option value="Graphics Designing">Graphics Designing</option>
          <option value="Data Analytics">Data Analytics</option>
          <option value="DSA in C++">DSA in C++</option>
          <option value="Java Programming">Java Programming</option>
          <option value="Python Programming">Python Programming</option>
        </select>
      </div>
      <div class="col-md-4">
        <input type="text" name="emp_id" class="form-control" placeholder="Employee ID" required>
      </div>
      <div class="col-md-4">
        <input type="text" name="phone" class="form-control" placeholder="Phone" required>
      </div>

      <!-- Third Row -->
      <div class="col-md-6">
        <input type="text" name="designation" class="form-control" placeholder="Designation" required>
      </div>
      <div class="col-md-6">
        <input type="text" name="img_url" class="form-control" placeholder="Image" required>
      </div>

      <!-- Button Centered -->
      <div class="col-12 text-center mt-3">
        <button class="btn btn-primary px-5">Create Admin</button>
      </div>
    </form>
  </div>
</section>

      <!-- View Admins -->
      <section id="viewAdmin" style="display:none;">
  <h4 class="section-title">List of Employees / Admin</h4>

  <div class="row row-cols-1 row-cols-md-1 g-3">
    <% admins.forEach(admin => { %>
      <div class="col">
        <div class="card shadow-sm " style="border-color: #ff6600 !important;">
          <div class="card-body d-flex align-items-center justify-content-between flex-wrap">

            <!-- Admin Info -->
            <div class="d-flex align-items-center gap-4 flex-wrap">
              <span class="fw-bold text-primary"><%= admin.name %></span>
              <span><strong>Email:</strong> <%= admin.email %></span>
              <span class="text-nowrap"><strong>Domain:</strong> <%= admin.domain %></span>
              <span><strong>Emp ID:</strong> <%= admin.emp_id %></span>
              <span><strong>Designation:</strong> <%= admin.designation %></span>
            </div>

            <!-- Buttons -->
            <div class="d-flex gap-1 mt-2 mt-md-0">
              <a href="/superAdmin/admin/<%= admin._id %>" class="btn btn-info btn-sm">View</a>
              <button class="btn btn-warning btn-sm" data-bs-toggle="modal" data-bs-target="#updateAdminModal<%= admin._id %>">Update</button>
              <button class="btn btn-danger btn-sm" data-bs-toggle="modal" data-bs-target="#deleteAdminModal<%= admin._id %>">Delete</button>
            </div>

          </div>
        </div>

        <!-- Update Admin Modal -->
        <div class="modal fade" id="updateAdminModal<%= admin._id %>" tabindex="-1" aria-hidden="true">
          <div class="modal-dialog">
            <div class="modal-content">
              <div class="modal-header">
                <h5 class="modal-title">Update <%= admin.name %></h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
              </div>
              <form action="/update-user/<%= admin._id %>" class="needs-validation" method="POST" novalidate>
                <div class="modal-body">
                  <label class="form-label">Name : </label>
                  <input type="text" name="name" class="form-control mb-2" value="<%= admin.name %>" required>
                  <label class="form-label">Email : </label>
                  <input type="email" name="email" class="form-control mb-2" value="<%= admin.email %>" required>
                  <label class="form-label">Emp Id : </label>
                  <input type="text" name="emp_id" class="form-control mb-2" value="<%= admin.emp_id %>" required>
                  <label class="form-label">Domain : </label>
                  <input type="text" name="domain" class="form-control mb-2" value="<%= admin.domain %>" required>
                  <label class="form-label">Phone : </label>
                  <input type="text" name="phone" class="form-control mb-2" value="<%= admin.phone %>" required>
                  <label class="form-label">Designation : </label>
                  <input type="text" name="designation" class="form-control mb-2" value="<%= admin.designation %>" required>
                  <label class="form-label">Profile Image : </label>
                  <input type="text" name="img_url" class="form-control mb-2" value="<%= admin.img_url %>" required>
                </div>
                <div class="modal-footer">
                  <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                  <button type="submit" class="btn btn-primary">Update</button>
                </div>
              </form>
            </div>
          </div>
        </div>

        <!-- Delete Admin Modal -->
        <div class="modal fade" id="deleteAdminModal<%= admin._id %>" tabindex="-1" aria-hidden="true">
          <div class="modal-dialog">
            <div class="modal-content">
              <div class="modal-header">
                <h5 class="modal-title">Delete <%= admin.name %>?</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
              </div>
              <div class="modal-body">Are you sure you want to delete this admin?</div>
              <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                <form action="/delete-user/<%= admin._id %>" method="POST">
                  <button class="btn btn-danger">Delete</button>
                </form>
              </div>
            </div>
          </div>
        </div>

      </div>
    <% }) %>
  </div>
</section>

      <!-- View Interns -->
      <section id="viewInterns" style="display:none;">
  <h4 class="section-title">List of Interns</h4>

  <!-- Filters in one line -->
  <div class="mb-3 d-flex flex-wrap align-items-center gap-2">
    <!-- Batch Filter -->
    <div class="flex-grow-1" style="min-width: 150px;">
      <select id="batchFilter" class="form-select" onchange="applyInternFilters()">
        <option value="all" selected>All Batches</option>
        <% batches.forEach(batch => { %>
          <option value="<%= batch %>">Batch <%= batch %></option>
        <% }) %>
      </select>
    </div>

    <!-- Intern Search -->
    <div class="flex-grow-2" style="min-width: 200px;">
      <input type="text" id="internSearchInput" class="form-control" placeholder="Search by Name or ID" onkeyup="applyInternFilters()">
    </div>

    <!-- Clear Filters -->
    <div>
      <button class="btn btn-secondary" onclick="clearInternFilters()">Clear Filters</button>
    </div>
  </div>

  <div class="row row-cols-1 row-cols-md-1 g-2" id="internCardsContainer">
    <% interns.forEach(intern => { %>
      <div class="col intern-card" data-batch="<%= intern.batch_no %>">
        <div class="card h-20 shadow-sm "style="border-color: #ff6600 !important;">
          <div class="card-body d-flex justify-content-between align-items-center">
            <div class="row w-100 align-items-center">
              <div class="col-md-3"><strong>Name:</strong> <span class="intern-name"><%= intern.name %></span></div>
              <div class="col-md-3"><strong>Email:</strong> <%= intern.email %></div>
              <div class="col-md-3"><strong>Domain:</strong> <%= intern.domain %></div>
              <div class="col-md-3"><strong>Intern ID:</strong> <span class="intern-id"><%= intern.intern_id %></span></div>
            </div>
            <div class="d-flex gap-2 ms-3">
              <a href="/admin/intern/<%= intern._id %>" class="btn btn-info btn-sm">View</a>
              <button class="btn btn-danger btn-sm" data-bs-toggle="modal" data-bs-target="#deleteInternModal<%= intern._id %>">Delete</button>
            </div>
          </div>
        </div>

        <!-- Delete Modal -->
        <div class="modal fade" id="deleteInternModal<%= intern._id %>" tabindex="-1" aria-hidden="true">
          <div class="modal-dialog">
            <div class="modal-content">
              <div class="modal-header">
                <h5 class="modal-title">Delete <%= intern.name %>?</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
              </div>
              <div class="modal-body">Are you sure you want to delete this intern?</div>
              <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                <form action="/delete-user/<%= intern._id %>" method="POST">
                  <button class="btn btn-danger">Delete</button>
                </form>
              </div>
            </div>
          </div>
        </div>
      </div>
    <% }) %>
  </div>
</section>


    </div>
  </div>
  <%- include("partials/toaster") %>
  <script src="/bootstrap/js/bootstrap.bundle.min.js"></script>
  <script src="/js/superAdmin.js"></script>
</body>
</html>
