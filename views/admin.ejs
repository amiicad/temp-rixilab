<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Admin Dashboard</title>
  <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">

<!-- Favicons -->
  <link href="img/Rixi Lab New Logo PNG.png" rel="icon">
  <link href="img/apple-touch-icon.png" rel="apple-touch-icon">   

  <style>
    body { overflow-x: hidden; background-color: #f0f2f5; }
    .sidebar { min-height: 100vh; background: #343a40; color: white; padding-top: 20px; }
    .sidebar a { color: white; text-decoration: none; display: flex; align-items: center; padding: 12px 20px; border-radius: 6px; margin-bottom: 5px; }
    .sidebar a:hover { background: #495057; }
    .sidebar i { margin-right: 10px; }
    .main-content { margin-left: 250px; padding: 30px; }
    /* .card:hover { transform: scale(1.03); transition: 0.3s; } */
    .badge-pending { background-color: #ffc107; color: black; }
    .badge-approved { background-color: #28a745; }
    .badge-rejected { background-color: #dc3545; }
    .table td, .table th { vertical-align: middle; }
    .section-title { font-weight: 600; margin-bottom: 15px; }
    .modal-header { background-color: #343a40; color: white; }
    .nav{background-color: #ffc197; color:black; }
    .sidebar a.active {
     background-color: #ff6600;  /* Bootstrap primary */
     color: #fff;
}

   /* .main-content {
  margin-left: 250px;
  padding: 30px;
  display: flex;
  flex-direction: column;
}
.main-content section {
  flex: 1;
} */
  </style>
</head>
<body>
<div class="d-flex flex-grow-1">
<%- include("partials/toaster") %>

  <!-- Sidebar -->
<div class="sidebar position-fixed w-20 d-flex flex-column justify-content-between" style="height: 100vh;">
  <div>
    <h2 class="text-center">
      <img src="/img/Rixi Lab New Logo PNG.png" width="35px" style="margin-bottom: 5px;" alt="Logo"> Rixi Lab
    </h2>
    <h4 class="text-center">Admin Panel</h4>
    <br/>
    <a href="#dashboard"><i class="bi bi-person-lines-fill"></i> Dashboard</a>
    <a href="#viewInterns"><i class="bi bi-people"></i> View Interns</a>
    <a href="#createUser"><i class="bi bi-person-plus"></i> Create Intern</a>
    <a href="#uploadProject"><i class="bi bi-upload"></i> Upload Project</a>
    <a href="#viewProjects"><i class="bi bi-folder2-open"></i> View Projects</a>
    <a href="#submittedProjects" style="margin-right: 4rem;"><i class="bi bi-folder2-open"></i> Submitted Project</a>
    <a href="/logout"><i class="bi bi-box-arrow-right"></i> Logout</a>
  </div>

  <!-- Footer at the bottom -->
  <div class="text-center mb-2" style="font-size: 0.85rem; color: #888;">
    © Copyright 2025 Rixi Lab.<br/> All Rights Reserved <br/><span style="font-size: smaller;">Developed by Anunay Anand (Director)</span>  
  </div>
</div>


  <!-- Main Content -->
  <div class="main-content flex-grow-1" >

    <!--Header Section -->
    <div class="header mb-4 w-100 !important  ">
      <h4 class="section-title mb-4 nav" style="padding: 30px; ">Welcome to Rixi Lab, <%= admin.name %>, <%=admin.designation%></h4>
    </div>

    
    <!-- Dashboard Section -->

      <section id="dashboard" class="py-4">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h4 class="fw-bold mb-0">Admin Dashboard</h4>
    <!-- Admin Profile -->
    <!-- <div class="d-flex align-items-center gap-3">
      <img src="<%= admin.img_url %>" 
           alt="Admin Image" 
           class="rounded-circle shadow-sm" 
           style="width:60px; height:60px; object-fit:cover;">

    </div> -->
  </div>

  <!-- Stats Row -->
  <div class="row g-4">
    <div class="col-md-3">
      <div class="card shadow-sm h-100 text-center p-3" style="border-color: #ff6600 !important;">
        <div class="card-body">
          <h6 class="text-muted">Total Interns</h6>
          <p class="display-5 fw-bold text-primary mb-0"><%= interns.length %></p>
        </div>
      </div>
    </div>
    <div class="col-md-3">
      <div class="card  shadow-sm h-100 text-center p-3" style="border-color: #ff6600 !important;">
        <div class="card-body">
          <h6 class="text-muted">Total Projects</h6>
          <p class="display-5 fw-bold text-success mb-0"><%= projects.length %></p>
        </div>
      </div>
    </div>
    <div class="col-md-3">
      <div class="card shadow-sm h-100 text-center p-3" style="border-color: #ff6600 !important;">
        <div class="card-body">
          <h6 class="text-muted">Total Batches</h6>
          <p class="display-5 fw-bold text-info mb-0"><%= batches.length %></p>
        </div>
      </div>
    </div>
    <div class="col-md-3">
      <div class="card  shadow-sm h-100 text-center p-3" style="border-color: #ff6600 !important;">
        <div class="card-body">
          <h6 class="text-muted">Certified Interns</h6>
          <p class="display-5 fw-bold text-info mb-0"><%= certifiedInternsCount %></p>
        </div>
      </div>
    </div>
    
<div class="container my-4">
  <div class="card shadow-sm">
    <div class="row g-0">

      <!-- Left: Image (rectangular, full height of card) -->
      <div class="col-md-4">
        <img src="<%= admin.img_url %>" 
             class="img-fluid h-100 w-100 rounded-start" 
             style="object-fit: cover;" 
             alt="User Image">
      </div>

      <!-- Right: Data -->
      <div class="col-md-8 p-3">
        <!-- Name takes full row -->
        <h4 class="card-title mb-3"><%= admin.name %></h4>

        <!-- Other Fields in 2-columns per row -->
        <div class="row">
          <div class="col-md-6">
            <p class="card-text"><strong>Email:</strong> <%= admin.email %></p>
          </div>
          <div class="col-md-6">
            <p class="card-text"><strong>Phone:</strong> <%= admin.phone %></p>
          </div>
          <div class="col-md-6">
            <p class="card-text"><strong>Domain:</strong> <%= admin.domain %></p>
          </div>
          <div class="col-md-6">
            <p class="card-text"><strong>Designation:</strong> <%= admin.designation %></p>
          </div>
          <div class="col-md-6">
            <p class="card-text"><strong>Employee ID:</strong> <%= admin.emp_id %></p>
          </div>
          <div class="col-md-6">
            <p class="card-text"><strong>Joining Date:</strong> <%= admin.joining_date %></p>
          </div>
        </div>

      </div>

    </div>
  </div>
</div>



  </div>
</section>



      <!-- Create Intern -->
   <section id="createUser" style="display:none;">
  <h4 class="section-title">Create Intern</h4>
  <div class="card p-4 shadow-sm mb-4">
    <form action="/create-user" method="POST" class="row g-3 needs-validation"  novalidate>
      <div class="col-md-6"><input type="text" name="name" class="form-control" placeholder="Name" required></div>
      <div class="col-md-6"><input type="email" name="email" class="form-control" placeholder="Email" required></div>

      <div class="col-md-6"><input type="text" name="password" class="form-control" placeholder="Password" required></div>
      <div class="col-md-6"><input type="text" name="domain" class="form-control" placeholder="<%= admin.domain %>" value="<%= admin.domain %>" readonly></div>

      <div class="col-md-6"><input type="text" name="phone" class="form-control" placeholder="Phone" required></div>
      <div class="col-md-6"><input type="text" name="intern_id" class="form-control" placeholder="Intern ID" required></div>

      <div class="col-md-6"><input type="text" name="university" class="form-control" placeholder="University" required></div>
      <div class="col-md-6"><input type="text" name="college" class="form-control" placeholder="College" required ></div>

      <div class="col-md-6"><input type="text" name="course" class="form-control" placeholder="Course" required></div>
      <div class="col-md-6"><input type="text" name="branch" class="form-control" placeholder="Branch" required></div>
      
      <!-- Year/Sem Dropdown -->
<div class="col-md-6">
  <select name="year_sem" class="form-select" required>
    <option value="" selected disabled>Select Year/Sem</option>
    <option value="1st/1sem">1st/1sem</option>
    <option value="1st/2sem">1st/2sem</option>
    <option value="2nd/1sem">2nd/1sem</option>
    <option value="2nd/2sem">2nd/2sem</option>
    <option value="3rd/1sem">3rd/1sem</option>
    <option value="3rd/2sem">3rd/2sem</option>
    <option value="4th/1sem">4th/1sem</option>
    <option value="4th/2sem">4th/2sem</option>
  </select>
</div>

<!-- Duration Dropdown -->
<div class="col-md-6">
  <select name="duration" class="form-select" required>
    <option value="" selected disabled>Select Duration (months)</option>
    <option value="4">4</option>
    <option value="6">6</option>
    <option value="8">8</option>
  </select>
</div>


      <div class="col-md-6"><input type="text" name="batch_no" class="form-control" placeholder="Batch" required></div>
 
      <div class="col-md-6"><input type="text" name="offer_letter" class="form-control" placeholder="Offer Letter Link" ></div>
      <div class="col-md-6 mx-auto "><button class="btn btn-primary w-100">Create</button></div>
    </form>
  </div>
</section>


    <!-- Upload Project -->
<section id="uploadProject" style="display:none;">
  <h4 class="section-title mb-4">Upload Project</h4>
  <div class="card shadow-sm border-0">
    <div class="card-body p-4">
      <form method="POST" action="/admin/projects" class="row g-4 needs-validation" novalidate>
        
        <!-- Row 1 -->
        <div class="col-md-6">
          <label class="form-label fw-semibold">Project Title</label>
          <input type="text" name="title" class="form-control" placeholder="Enter project title" required>
        </div>
        
        <div class="col-md-6">
          <label class="form-label fw-semibold">Description</label>
          <input type="text" name="description" class="form-control" placeholder="Enter short description" required>
        </div>

        <!-- Row 2 -->
        <div class="col-md-6">
          <label class="form-label fw-semibold">Download Link</label>
          <input type="url" name="downloadLink" class="form-control" placeholder="Paste download link" required>
        </div>
        
        <div class="col-md-6">
          <label class="form-label fw-semibold">Upload Link</label>
          <input type="url" name="uploadLink" class="form-control" placeholder="Paste upload link" required>
        </div>

        <!-- Row 3 -->
        <div class="col-md-6">
          <label class="form-label fw-semibold">Batch</label>
          <select name="batch_no" class="form-select" required>
            <option value="" disabled selected>Select Batch</option>
            <% batches.forEach(b => { %>
              <option value="<%= b %>"><%= b %></option>
            <% }) %>
          </select>
        </div>

        <div class="col-md-6">
          <label class="form-label fw-semibold">Week</label>
          <select name="week" class="form-select" required>
            <option value="" disabled selected>Select Week</option>
            <% for (let i = 1; i <= 8; i++) { %>
              <% if (i !== 5 && i !== 7) { %>
                <option value="<%= i %>">Week <%= i %></option>
              <% } %>
            <% } %>
          </select>
        </div>

        <!-- Button Row -->
        <div class="col-12 text-center mt-3">
          <button class="btn btn-primary px-5 py-2 ">
            Create Project
          </button>
        </div>

      </form>
    </div>
  </div>
</section>




    <!-- View Interns (Default) -->
<section id="viewInterns" style="display:none;">
  <h4 class="section-title">Interns <%= admin.domain %></h4>

  <!-- Filters -->
  <div class="mb-3 d-flex align-items-center flex-wrap gap-3">
    <!-- Batch Filter -->
    <div style="min-width: 250px;">
      <!-- <label for="batchFilter" class="form-label">Select Batch</label> -->
      <select id="batchFilter" class="form-select" onchange="applyInternFilters()">
        <option value="all" selected>All Batches</option>
        <% batches.forEach(batch => { %>
          <option value="<%= batch %>">Batch <%= batch %></option>
        <% }) %>
      </select>
    </div>

    <!-- Intern Search -->
    <div>
      <!-- <label for="internSearch" class="form-label">Search by Intern ID</label> -->
      <input type="text" id="internSearch" class="form-control" placeholder="Enter Intern ID" onkeyup="applyInternFilters()">
    </div>

    <!-- Clear Filters -->
    <div class="align-self-end">
      <button class="btn btn-secondary " onclick="clearInternFilters()">Clear Filters</button>
    </div>
  </div>

  <!-- Intern Cards -->
  <div class="row row-cols-1 row-cols-md-1 g-8">
    <% interns.forEach(i => { %>
      <div class="col  intern-card" data-batch="<%= i.batch_no %>">
        <div class="card shadow-sm mb-3">
          <div class="card-body">
            <div class="row w-100 align-items-center">
              <div class="col-md-3"><strong>Name:</strong> <%= i.name %></div>
              <div class="col-md-3"><strong>Email:</strong> <%= i.email %></div>
              <div class="col-md-2"><strong>Duration:</strong> <%= i.duration %> weeks</div>
              <div class="col-md-2"><strong>Intern ID:</strong> <span class="intern-id d-none"><%= i.intern_id %></span><%= i.intern_id %></div>
              <!-- Buttons on the same line -->
              <div class="col-md-2 d-flex gap-2 justify-content-end">
                <a href="/admin/intern/<%= i._id %>" class="btn btn-info btn-sm">View</a>
                <button class="btn btn-warning btn-sm" data-bs-toggle="modal" data-bs-target="#updateModal<%= i._id %>">Update</button>
              </div>
            </div>
          </div>
        </div>

        <!-- Update Intern Modal -->
        <div class="modal fade" id="updateModal<%= i._id %>" tabindex="-1" aria-hidden="true">
  <div class="modal-dialog modal-lg">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Update <%= i.name.trim() %></h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
      </div>
      <form action="/update-user/<%= i._id %>" method="POST" class="needs-validation" novalidate>
        <div class="modal-body">
          <div class="row">
            <!-- Name + Email -->
            <div class="col-md-6 mb-2">
              <label class="form-label">Name:</label>
              <input type="text" name="name" class="form-control" value="<%= i.name %>" required>
            </div>
            <div class="col-md-6 mb-2">
              <label class="form-label">Email:</label>
              <input type="email" name="email" class="form-control" value="<%= i.email %>" required>
            </div>

            <!-- Phone + College -->
            <div class="col-md-6 mb-2">
              <label class="form-label">Phone:</label>
              <input type="text" name="phone" class="form-control" value="<%= i.phone %>">
            </div>
            <div class="col-md-6 mb-2">
              <label class="form-label">College:</label>
              <input type="text" name="college" class="form-control" value="<%= i.college %>">
            </div>

            <!-- Batch No + Certificate ID -->
            <div class="col-md-6 mb-2">
              <label class="form-label">Batch No:</label>
              <input type="text" name="batch_no" class="form-control" value="<%= i.batch_no %>" readonly>
            </div>
            <div class="col-md-6 mb-2">
              <label class="form-label">Certificate ID:</label>
              <input type="text" name="certificate_id" class="form-control" value="<%= i.certificate_id %>">
            </div>

            <!-- University + Course -->
            <div class="col-md-6 mb-2">
              <label class="form-label">University:</label>
              <input type="text" name="university" class="form-control" value="<%= i.university %>">
            </div>
            <div class="col-md-6 mb-2">
              <label class="form-label">Course:</label>
              <input type="text" name="course" class="form-control" value="<%= i.course %>">
            </div>

            <!-- Branch + Year/Sem -->
            <div class="col-md-6 mb-2">
              <label class="form-label">Branch:</label>
              <input type="text" name="branch" class="form-control" value="<%= i.branch %>">
            </div>
            <div class="col-md-6 mb-2">
              <label class="form-label">Y/S:</label>
              <input type="text" name="year_sem" class="form-control" value="<%= i.year_sem %>">
            </div>

            <!-- Duration + Intern ID -->
            <div class="col-md-6 mb-2">
              <label class="form-label">Duration:</label>
              <input type="text" name="duration" class="form-control" value="<%= i.duration%>">
            </div>
            <div class="col-md-6 mb-2">
              <label class="form-label">Intern ID:</label>
              <input type="text" name="intern_id" class="form-control" value="<%= i.intern_id %>" readonly>
            </div>

            <!-- Offer Letter + Certificate Link -->
            <div class="col-md-6 mb-2">
              <label class="form-label">Offer Letter Link:</label>
              <input type="text" name="offer_letter" class="form-control" value="<%= i.offer_letter %>">
            </div>
            <div class="col-md-6 mb-2">
              <label class="form-label">Certificate Link:</label>
              <input type="text" name="certificate_link" class="form-control" value="<%= i.certificate_link %>">
            </div>
          </div>
        </div>
        <div class="modal-footer justify-content-center">
          <button type="submit" class="btn btn-warning btn-sm">Update</button>
          <button type="button" class="btn btn-danger btn-sm" data-bs-dismiss="modal">Close</button>
        </div>
      </form>
    </div>
  </div>
</div>

      </div>
    <% }) %>
  </div>
</section>



    <!-- View Projects -->
<section id="viewProjects" style="display:none;">
  <h4 class="section-title">Projects</h4>

  <!-- Batch Dropdown -->
  <div class="mb-3">
    <label for="projectBatchFilter" class="form-label">Select Batch</label>
    <select id="projectBatchFilter" class="form-select" onchange="filterProjectsByBatch()">
      <option value="all" selected>All Batches</option>
      <% batches.forEach(batch => { %>
        <option value="<%= batch %>">Batch <%= batch %></option>
      <% }) %>
    </select>
  </div>

  <!-- Project Cards -->
  <div class="row row-cols-1 row-cols-md-1 g-8">
    <% projects.forEach(p => { %>
      <div class="col project-card" data-batch="<%= p.batch_no %>">
        <div class="card shadow-sm border-primary mb-3">
          <div class="card-body d-flex justify-content-between align-items-center">

            <!-- Left side: all details in one line -->
            <div class="d-flex gap-4 align-items-center">
              <span><strong>Title:</strong> <%= p.title %></span>
              <span><strong>Week:</strong> <%= p.week %></span>
              <span><strong>Batch:</strong> <%= p.batch_no %></span>
              <span><strong>Description:</strong> <%= p.domain %></span>
            </div>

            <!-- Right side: buttons -->
            <div class="d-flex gap-2">
              <!-- Update Button -->
              <button class="btn btn-warning btn-sm" data-bs-toggle="modal" data-bs-target="#updateModal<%= p._id %>">
                Update
              </button>

              <!-- Delete Button -->
              <button class="btn btn-danger btn-sm" data-bs-toggle="modal" data-bs-target="#deleteProjectModal<%= p._id %>">
                Delete
              </button>
            </div>

          </div>
        </div>

        <!-- Update Modal -->
        <div class="modal fade" id="updateModal<%= p._id %>" tabindex="-1" aria-hidden="true">
          <div class="modal-dialog">
            <div class="modal-content">
              <div class="modal-header">
                <h5 class="modal-title">Update <%= p.title %></h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
              </div>
              <form action="/admin/project/update/<%= p._id %>" method="POST">
                <div class="modal-body">
                  <label class="form-label">Title :</label>
                  <input type="text" name="title" class="form-control mb-2" value="<%= p.title %>" required>

                  <label class="form-label">Description :</label>
                  <textarea name="description" class="form-control mb-2"><%= p.description %></textarea>

                  <label class="form-label">Domain :</label>
                  <input type="text" name="domain" class="form-control" value="<%= p.domain %>">

                  <label class="form-label">Week :</label>
                  <input type="number" name="week" class="form-control" value="<%= p.week %>" min="1" max="8" required>

                  <label class="form-label">Batch :</label>
                  <input type="number" name="batch_no" class="form-control" value="<%= p.batch_no %>">

                  <label class="form-label">Upload Link :</label>
                  <input type="text" name="uploadLink" class="form-control" value="<%= p.uploadLink %>">

                  <label class="form-label">Download Link :</label>
                  <input type="text" name="downloadLink" class="form-control" value="<%= p.downloadLink %>">
                </div>
                <div class="modal-footer">
                  <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                  <button type="submit" class="btn btn-primary">Update</button>
                  <button type="button" class="btn btn-danger btn-sm" data-bs-dismiss="modal">Close</button>
                </div>
              </form>
            </div>
          </div>
        </div>

        <!-- Delete Modal -->
        <div class="modal fade" id="deleteProjectModal<%= p._id %>" tabindex="-1" aria-hidden="true">
          <div class="modal-dialog">
            <div class="modal-content">
              <div class="modal-header">
                <h5 class="modal-title">Delete <%= p.title %>?</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
              </div>
              <div class="modal-body">
                Are you sure you want to delete this project?
              </div>
              <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                <form action="/delete-project/<%= p._id %>" method="POST" style="display:inline;">
                  <button class="btn btn-danger" type="submit">Delete</button>
                </form>
              </div>
            </div>
          </div>
        </div>

      </div>
    <% }) %>
  </div>
</section>




    <!-- Submitted Projects -->


<section id="submittedProjects" style="display:none;">
  <h4 class="section-title">Submitted Projects</h4>

  <% var currentSubmittedBatch = typeof submittedBatch !== 'undefined' ? submittedBatch : 'all'; %>

  <!-- Filters -->
  <div class="mb-3 d-flex align-items-center flex-wrap gap-3">
    <!-- Batch Filter -->
    <div>
      <select class="form-select" id="submittedBatchFilter" onchange="applySubmittedFilters()">
        <option value="all" <%= currentSubmittedBatch === 'all' ? 'selected' : '' %>>All Batches</option>
        <% batches.forEach(batch => { %>
          <option value="<%= batch %>" <%= currentSubmittedBatch === batch ? 'selected' : '' %>>Batch <%= batch %></option>
        <% }) %>
      </select>
    </div>

    <!-- Intern Search (UNIQUE ID) -->
    <div>
      <input type="text" id="submittedInternSearch" class="form-control"
             placeholder="Enter Intern ID" onkeyup="applySubmittedFilters()">
    </div>

    <!-- Clear Filters -->
    <div class="align-self-end">
      <button class="btn btn-secondary" onclick="clearSubmittedFilters()">Clear Filters</button>
    </div>
  </div>

  <div class="row row-cols-1 row-cols-md-1 g-8">
    <% interns.forEach(intern => { %>
      <% if (Array.isArray(intern.projectAssigned) && intern.projectAssigned.length > 0) { %>
        <% if (currentSubmittedBatch === 'all' || intern.batch_no === currentSubmittedBatch) { %>
          <div class="col mb-3 intern-card" data-batch="<%= intern.batch_no %>">
            <div class="card shadow-sm" style="border-color: #ff6600 !important;">
              <div class="card-body">
                <div class="d-flex justify-content-between align-items-center mb-3">
                  <span class="h5 mb-0"><%= intern.name %> (<%= intern.batch_no %>)</span>
                  <span>
                    <b>Email:</b> <%= intern.email %>&nbsp;&nbsp;
                    <b>Intern ID:</b>
                    <span class="intern-id d-none"><%= intern.intern_id %></span>
                    <%= intern.intern_id %>
                  </span>
                </div>

                <table class="table table-bordered">
                  <thead>
                    <tr style="text-align:center;">
                      <th>Project Title</th>
                      <th>Week</th>
                      <th>Status</th>
                      <th>Update Status</th>
                    </tr>
                  </thead>
                  <tbody style="text-align:center;">
                    <% intern.projectAssigned.forEach(sub => { %>
                      <tr>
                        <td><%= sub.projectId ? sub.projectId.title : 'N/A' %></td>
                        <td>Week <%= sub.week %></td>
                        <td>
                          <%
                            let badgeClass = 'badge-pending';
                            if (sub.status === 'accepted') badgeClass = 'badge-approved';
                            else if (sub.status === 'rejected') badgeClass = 'badge-rejected';
                          %>
                          <span class="badge <%= badgeClass %>"><%= sub.status %></span>
                        </td>
                        <td>
                          <form method="POST" action="/admin/projects/update-status" class="d-flex align-items-center gap-2">
                            <input type="hidden" name="userId" value="<%= intern._id %>">
                            <input type="hidden" name="projectId" value="<%= sub.projectId ? sub.projectId._id : sub.projectId %>">
                            <select name="status" class="form-select form-select-sm">
                              <option value="accepted" <%= sub.status === 'accepted' ? 'selected' : '' %>>Approve</option>
                              <option value="rejected" <%= sub.status === 'rejected' ? 'selected' : '' %>>Reject</option>
                            </select>
                            <button type="submit" class="btn btn-primary btn-sm">Update</button>
                          </form>
                        </td>
                      </tr>
                    <% }) %>
                  </tbody>
                </table>

              </div>
            </div>
          </div>
        <% } %>
      <% } %>
    <% }) %>
  </div>
</section>



<!-- JS for Filters -->




   


  </div>
</div>

<script src="/bootstrap/js/bootstrap.bundle.min.js"></script>
<script src="/js/admi.js"></script>
<script>

const sidebarLinks = document.querySelectorAll('.sidebar a[href^="#"]');
const sections = document.querySelectorAll('.main-content section');

// Initially show only dashboard
sections.forEach(sec => {
  sec.style.display = sec.id === 'dashboard' ? 'block' : 'none';
});

sidebarLinks.forEach(link => {
  link.addEventListener('click', e => {
    e.preventDefault();
    const targetId = link.getAttribute('href').substring(1);

    // Hide all sections, including dashboard
    sections.forEach(sec => sec.style.display = 'none');

    // Show only the clicked section
    const targetSection = document.getElementById(targetId);
    if (targetSection) targetSection.style.display = 'block';
  });
});



  function toggleConfirmAll() {
    const checked = document.getElementById('selectAllConfirm').checked;
    document.querySelectorAll('.confirmCheckbox').forEach(c => c.checked = checked);
  }
  function toggleCongratsAll() {
    const checked = document.getElementById('selectAllCongrats').checked;
    document.querySelectorAll('.congratsCheckbox').forEach(c => c.checked = checked);
  }

function filterByBatch() {
    const selectedBatch = document.getElementById("batchFilter").value;
    const cards = document.querySelectorAll(".intern-card");

    cards.forEach(card => {
      if (selectedBatch === "all" || card.dataset.batch === selectedBatch) {
        card.style.display = "block";
      } else {
        card.style.display = "none";
      }
    });
  }
  function filterProjectsByBatch() {
    const selectedBatch = document.getElementById("projectBatchFilter").value;
    const cards = document.querySelectorAll(".project-card");

    cards.forEach(card => {
      if (selectedBatch === "all" || card.dataset.batch === selectedBatch) {
        card.style.display = "block";
      } else {
        card.style.display = "none";
      }
    });
  }
    // Client-side batch filter
  function filterSubmittedProjects(batch) {
    const cards = document.querySelectorAll('#submittedProjects .intern-card');
    cards.forEach(card => {
      const internBatch = card.dataset.batch;
      card.style.display = (batch === 'all' || internBatch === batch) ? 'block' : 'none';
    });
  }

  function applyFilters() {
  let batchFilter = document.getElementById("submittedBatchFilter").value.toLowerCase();
  let internSearch = document.getElementById("internSearch").value.toLowerCase();
  let cards = document.querySelectorAll("#submittedProjects .intern-card");

  cards.forEach(card => {
    let cardBatch = card.getAttribute("data-batch").toLowerCase();
    let internId = card.querySelector(".intern-id")?.textContent.toLowerCase() || "";

    let matchesBatch = (batchFilter === "all" || cardBatch === batchFilter);
    let matchesIntern = (internSearch === "" || internId.includes(internSearch));

    // Show only if BOTH match
    card.style.display = (matchesBatch && matchesIntern) ? "block" : "none";
  });
}

function clearFilters() {
  document.getElementById("submittedBatchFilter").value = "all";
  document.getElementById("internSearch").value = "";
  applyFilters();
}

// Intern Filters
function applyInternFilters() {
  let batchFilter = document.getElementById("batchFilter").value.toLowerCase();
  let internSearch = document.getElementById("internSearch").value.toLowerCase();
  let cards = document.querySelectorAll("#viewInterns .intern-card");

  cards.forEach(card => {
    let cardBatch = card.getAttribute("data-batch").toLowerCase();
    let internId = card.querySelector(".intern-id")?.textContent.toLowerCase() || "";

    let matchesBatch = (batchFilter === "all" || cardBatch === batchFilter);
    let matchesIntern = (internSearch === "" || internId.includes(internSearch));

    card.style.display = (matchesBatch && matchesIntern) ? "block" : "none";
  });
}

function clearInternFilters() {
  document.getElementById("batchFilter").value = "all";
  document.getElementById("internSearch").value = "";
  applyInternFilters();
}


document.querySelectorAll('.sidebar a').forEach(link => {
  link.addEventListener('click', () => {
    document.querySelectorAll('.sidebar a').forEach(l => l.classList.remove('active'));
    link.classList.add('active');
  });
});


  // Run once on page load to hide all
  window.addEventListener('DOMContentLoaded', (event) => {
    filterByBatch();
    filterProjectsByBatch();
    filterSubmittedProjects('<%= currentSubmittedBatch %>');
  });


    // Scoped helpers (Submitted Projects only)
  function getSubmittedSection() {
    return document.getElementById('submittedProjects');
  }

  function filterSubmittedByBatch(card, batchFilter) {
    const cardBatch = (card.getAttribute('data-batch') || '').toLowerCase();
    return (batchFilter === 'all' || cardBatch === batchFilter);
  }

  function filterSubmittedByIntern(card, internSearch) {
    const internId = (card.querySelector('.intern-id')?.textContent || '').toLowerCase().trim();
    return (internSearch === '' || internId.includes(internSearch));
  }

  function applySubmittedFilters() {
    const section = getSubmittedSection();
    const batchFilter = section.querySelector('#submittedBatchFilter').value.toLowerCase();
    const internSearch = section.querySelector('#submittedInternSearch').value.toLowerCase().trim();
    const cards = section.querySelectorAll('.intern-card');

    cards.forEach(card => {
      const matchesBatch = filterSubmittedByBatch(card, batchFilter);
      const matchesIntern = filterSubmittedByIntern(card, internSearch);
      card.style.display = (matchesBatch && matchesIntern) ? '' : 'none';
    });
  }

  function clearSubmittedFilters() {
    const section = getSubmittedSection();
    section.querySelector('#submittedBatchFilter').value = 'all';
    section.querySelector('#submittedInternSearch').value = '';
    applySubmittedFilters();
  }

  document.addEventListener('DOMContentLoaded', applySubmittedFilters);

  (() => {
    'use strict'

    // Fetch all the forms we want to apply custom Bootstrap validation styles to
    const forms = document.querySelectorAll('.needs-validation')

    // Loop over them and prevent submission
    Array.from(forms).forEach(form => {
      form.addEventListener('submit', event => {
        if (!form.checkValidity()) {
          event.preventDefault()
          event.stopPropagation()
        }

        form.classList.add('was-validated')
      }, false)
    })
  })()
</script>
</body>
</html>